    <%
      let objh = {
        'admin': {full: 'Admin', short: 'A', link: '/admin'},
        'manager': {full: 'Manager', short: 'M', link: '/manager'},
        'user': {full: 'User', short: 'U', link: '#'},
        'pending-user': {full: 'User', short: 'U', link: '#'},
      }
    %>

    <!-- Logo -->
    <!--<a href="<%= (user.level in objh) ? objh[user.level].link : '#' %>" class="logo">-->
    <a href="/home" class="logo">
      
      <!-- mini logo for sidebar mini 50x50 pixels -->
      <!--<span class="logo-mini"><b><%= (user.level in objh) ? objh[user.level].short : 'U' %></b></span>-->
      <span class="logo-mini"><b>T</b></span>
      <!-- logo for regular state and mobile devices -->
      <!--<span class="logo-lg"><b><%= (user.level in objh) ? objh[user.level].full : 'User' %></b></span>-->
      <span class="logo-lg"><b>Trang chủ</b></span>
    </a>
    <!-- Header Navbar: style can be found in header.less -->
    <nav class="navbar navbar-static-top">
      <!-- Sidebar toggle button-->
      <a href="#" class="sidebar-toggle" data-toggle="offcanvas" role="button">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>

      <div class="navbar-custom-menu">
        <ul class="nav navbar-nav">
          
          <!-- User Account: style can be found in dropdown.less -->
          <li class="dropdown user user-menu">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <img src="<%= (('avatar' in user) && ('original' in user.avatar) && (user.avatar.original)) ? ('/' + user.avatar.original) : '/admin/dist/img/user4-128x128.jpg' %>" class="user-image" alt="User Image">
              <span class="hidden-xs"><%= user.fullname %></span>
            </a>
            <ul class="dropdown-menu">
              <!-- User image -->
              <li class="user-header">
                <img src="<%= (('avatar' in user) && ('original' in user.avatar) && (user.avatar.original)) ? ('/' + user.avatar.original) : '/admin/dist/img/user4-128x128.jpg' %>" class="img-circle" alt="User Image" onclick="window.location.href = '/users/me'" style="cursor: pointer">

                <p>
                  <%= user.fullname %>
                  <small id="userLevel"></small>
                  <small id="userMaDeTai"><%= user.maDeTai %></small>
                  <script>
                    
                  </script>
                  <!--<small>Member since Nov. 2012</small>-->
                </p>

              </li>
              
              <!-- Menu Footer-->
              <li class="user-body bg-success">
                <div class="pull-left">
                  <a href="/home" class="btn btn-success btn-flat">Trang chủ</a>
                </div>
                <div class="pull-right">
                  <a href="/auth/logout" class="btn btn-warning btn-flat">Đăng xuất</a>
                </div>
              </li>
            </ul>
          </li>
          <!-- Control Sidebar Toggle Button -->
          
        </ul>
      </div>
    </nav>